# Copy this file to backend/.env (or export these as real environment
# variables) and fill in real values. Never commit the actual .env file.

DATABASE_URL=mysql+pymysql://root:your_password@localhost/last_safe_moment
SECRET_KEY=change_this_to_a_long_random_string
